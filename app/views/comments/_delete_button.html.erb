<% if current_user.id == idea.user_id %>
  <%= link_to idea, method: :delete, data: { confirm: "You sure?" }, class: 'btn btn-danger btn-xs del' do %>
      <%= content_tag :span, "", class: "glyphicon glyphicon-trash" %>
  <% end %>
<% end %>